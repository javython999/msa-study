server:
  port: 0

spring:
  application:
    name: user-service

  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest

  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update

  security:
    user:
      name: user
      password: $2a$10$CszASEQIYrwvh9yHWYrP0OcfjkONTt1U1pfo3dmVUYGashO1eaR4S

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
    fetch-registry: true
    register-with-eureka: true

greeting:
  message: Welcome to the Simple E-commerce

management:
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include:
          - refresh
          - busrefresh
          - health
          - beans
          - info
          - httpexchanges

logging:
  level:
    org.springframework.security: DEBUG
    com.errday.userservice.client: DEBUG